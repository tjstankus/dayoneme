require 'spec_helper'

describe Dayoneme do
  it 'should have a version number' do
    expect(Dayoneme::VERSION).not_to be nil
  end

  it 'should do something useful' do
    expect(false).to eq(true)
  end
end
